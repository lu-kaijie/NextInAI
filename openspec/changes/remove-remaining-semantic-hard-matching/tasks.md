## 1. 清理 AssistantAgent 语义硬匹配

- [x] 1.1 盘点并删除 `AssistantAgent` 中所有自然语言参数提取、语义映射和关键词补参逻辑
- [x] 1.2 保留并收敛引用解析，只允许解析结构化上下文引用，不再承担业务参数补全
- [x] 1.3 清理与旧补参逻辑耦合的异常、状态恢复和辅助函数

## 2. 收紧工具参数契约

- [x] 2.1 梳理 harness tool registry 的输入 schema，确保关键能力的业务参数显式可见
- [x] 2.2 为缺失字段、类型错误、格式错误、枚举越界补齐统一结构化错误对象
- [x] 2.3 调整确认流，确保副作用动作在进入确认门前先完成参数校验

## 3. 打通单路径 agent loop

- [x] 3.1 调整 agent loop 执行链路，使 tool 参数仅来自 agent 决策或结构化引用解析
- [x] 3.2 让参数校验错误可回传给模型继续修正，而不是由程序继续猜测
- [x] 3.3 删除 chat 侧剩余 fallback / 关键词路由 / 参数兜底分支

## 4. 对齐跨入口行为

- [x] 4.1 校验 chat、web、CLI 对相同能力使用同一后端参数边界和错误语义
- [x] 4.2 清理入口层任何额外的自然语言补参逻辑

## 5. 测试与文档

- [x] 5.1 补充或重写测试，覆盖显式参数成功、参数缺失失败、非法参数失败、引用解析成功等路径
- [x] 5.2 更新 README / 使用文档 / 变更说明，明确程序不再做自然语言参数推断
